# cpu-cache-timing-experiment
A C program that measures CPU memory access latency across L1, L2, L3 cache and RAM to demonstrate the timing side-channel principle underlying CPU-GPU security vulnerabilities.
